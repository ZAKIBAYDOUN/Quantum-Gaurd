name: ci
on:
  push:
    branches: [main, import/supercore]
  pull_request:
    branches: [main]
jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo \"CI OK\"